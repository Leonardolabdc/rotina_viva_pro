{
  "name": "@rotina-viva/api-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Contrato OpenAPI compartilhado entre apps/web e apps/api",
  "scripts": {
    "validate": "redocly lint openapi.yaml",
    "generate:types": "openapi-typescript openapi.yaml -o ./generated/schema.d.ts",
    "generate": "pnpm run validate && pnpm run generate:types"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.3",
    "openapi-typescript": "^7.8.0"
  },
  "files": [
    "openapi.yaml",
    "generated"
  ]
}
