# Voz (STT) — OpenRouter (recomendado no Streamlit Cloud; mesma OPENROUTER_API_KEY)
OPENAI_TRANSCRIBE_BASE_URL = "https://openrouter.ai/api/v1"
OPENAI_TRANSCRIBE_MODEL = "openai/whisper-1"

# Alternativa: Whisper self-hosted (VM Oracle, Docker local)
# OPENAI_TRANSCRIBE_BASE_URL = "http://203.0.113.10:9000/v1"
# OPENAI_TRANSCRIBE_MODEL = "whisper-1"

# Indexação RAG mais rápida no 1.º arranque (opcional)