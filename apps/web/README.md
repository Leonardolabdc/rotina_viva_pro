# apps/web — Next.js (Fase 5)

Placeholder do frontend profissional na Vercel.

## Próximos passos (Fase 5)

1. `pnpm create next-app . --typescript --app --tailwind --eslint`
2. Gerar tipos: `pnpm contracts:generate` (raiz do monorepo)
3. Consumir API via SSE em `/chat/sessions/{id}/messages/stream`
4. Auth com Supabase client (Fase 1)

## Contrato

Tipos e rotas definidos em [`packages/api-contracts`](../packages/api-contracts/).
